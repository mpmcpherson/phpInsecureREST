<?php

namespace Tests\Unit\REST_API;

use REST_API\CouchDB;
use Tests\TestCase;

/**
 * Class CouchDBTest.
 *
 * @covers \REST_API\CouchDB
 */
class CouchDBTest extends TestCase
{
    /**
     * @var CouchDB
     */
    protected $couchDB;

    /**
     * @var mixed
     */
    protected $db;

    /**
     * @var mixed
     */
    protected $host;

    /**
     * @var mixed
     */
    protected $port;

    /**
     * @var mixed
     */
    protected $username;

    /**
     * @var mixed
     */
    protected $password;

    /**
     * {@inheritdoc}
     */
    protected function setUp(): void
    {
        parent::setUp();

        $this->db = null;
        $this->host = null;
        $this->port = null;
        $this->username = null;
        $this->password = null;
        $this->couchDB = new CouchDB($this->db, $this->host, $this->port, $this->username, $this->password);
    }

    /**
     * {@inheritdoc}
     */
    protected function tearDown(): void
    {
        parent::tearDown();

        unset($this->couchDB);
        unset($this->db);
        unset($this->host);
        unset($this->port);
        unset($this->username);
        unset($this->password);
    }

    public function testDecode_json(): void
    {
        /** @todo This test is incomplete. */
        $this->markTestIncomplete();
    }

    public function testEncode_json(): void
    {
        /** @todo This test is incomplete. */
        $this->markTestIncomplete();
    }

    public function testSend(): void
    {
        /** @todo This test is incomplete. */
        $this->markTestIncomplete();
    }

    public function testGet_all_docs(): void
    {
        /** @todo This test is incomplete. */
        $this->markTestIncomplete();
    }

    public function testGet_item(): void
    {
        /** @todo This test is incomplete. */
        $this->markTestIncomplete();
    }
}
